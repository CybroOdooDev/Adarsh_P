<templates id="template" xml:space="preserve">
        <t t-inherit="web.NavBar" t-inherit-mode="extension" owl="1">
        <xpath expr="//nav[hasclass('o_main_navbar')]" position="replace">
            <nav class="o_main_navbar navbar navbar-expand-lg fixed-top d-print-none"
                 data-command-category="disabled">
                <div class="container-fluid">
                    <!-- Apps Menu (Preserving Odoo functionality) -->
                    <t t-call="web.NavBar.AppsMenu">
                        <t t-set="apps" t-value="menuService.getApps()"/>
                    </t>
                    <!-- App Brand Logo & Name -->
                    <t t-if="currentApp">
                        <a class="navbar-brand d-flex align-items-center gap-2" 
                           t-att-href="getMenuItemHref(currentApp)"
                           t-att-data-menu-xmlid="currentApp.xmlid"
                           t-att-data-section="currentApp.id"
                           t-on-click.prevent="() => this.onNavBarDropdownItemSelection(currentApp)"
                           style="margin-left: 10px;">
                            <img t-if="currentApp.webIconData" 
                                 t-att-src="currentApp.webIconData" 
                                 alt="App Icon" 
                                 style="height: 24px; width: 24px; object-fit: contain;"/>
                            <span class="fs-6 fw-bold" t-esc="currentApp.name" style="color: var(--text-color);"/>
                        </a>
                    </t>
                    <div class="o_navbar_breadcrumbs d-contents"/>
                    <div class="collapse navbar-collapse" id="navbarSupportedContent" t-ref="menu_sections" >
                        <!-- App Sections (Preserving Odoo functionality) -->
                        <t t-if="!env.isSmall">
                            <t t-if="currentAppSections.length"
                               t-call="web.NavBar.SectionsMenu">
                                <t t-set="sections" t-value="currentAppSections"/>
                            </t>
                        </t>
                    </div>
                    <div class="o_menu_systray d-flex flex-shrink-0 ms-auto"
                         role="menu">
                          <t t-foreach="systrayItems" t-as="item" t-key="item.key">
                            <!-- This ensures the correct order of the systray items -->
                              <div t-att-data-index="item.index"/>
                              <ErrorHandler
                                      onError="error => this.handleItemError(error, item)">
                                <t t-component="item.Component" t-props="item.props"/>
                            </ErrorHandler>
                          </t>
                    </div>
                </div>
            </nav>
        </xpath>
    </t>
</templates>